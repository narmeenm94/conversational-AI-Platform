# Core ML / GPU
torch>=2.1.0
torchaudio>=2.1.0
accelerate>=0.30.0

# Pipeline Orchestration (includes whisper and ollama integrations)
pipecat-ai[whisper,websocket]>=0.0.52
ollama>=0.4.0
websockets>=12.0

# Text-to-Speech — Orpheus via vllm + SNAC
# vllm runs as a separate server process (started by start_cloud.sh)
# SNAC decodes vllm token output into audio
snac>=1.0.0
openai>=1.0.0

# Text-to-Speech — Orpheus (community package, still used for tokenizer reference)
# Install from GitHub:  pip install git+https://github.com/Deathdadev/Orpheus-Speech-PyPi

# Speech-to-Text (pulled in by pipecat-ai[whisper], listed for clarity)
faster-whisper>=1.0.0

# RAG / Knowledge Base
chromadb>=0.5.0
sentence-transformers>=3.0.0

# Sentiment / Emotion (optional)
transformers>=4.40.0

# Configuration
python-dotenv>=1.0.0

# Document ingestion helpers
pypdf>=4.0.0
python-docx>=1.0.0
